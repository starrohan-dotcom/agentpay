global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "agentpay"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["agentpay:3000"]
        labels:
          service: "agentpay"
          environment: "production"